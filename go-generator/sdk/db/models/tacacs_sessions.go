// Code generated by SQLBoiler 4.18.0 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// TacacsSession is an object representing the database table.
type TacacsSession struct {
	ID         int64       `boil:"id" json:"id" toml:"id" yaml:"id"`
	Server     null.String `boil:"server" json:"server,omitempty" toml:"server" yaml:"server,omitempty"`
	User       null.String `boil:"user" json:"user,omitempty" toml:"user" yaml:"user,omitempty"`
	IPAddr     null.String `boil:"ip_addr" json:"ip_addr,omitempty" toml:"ip_addr" yaml:"ip_addr,omitempty"`
	Shared     null.String `boil:"shared" json:"shared,omitempty" toml:"shared" yaml:"shared,omitempty"`
	Started    null.Time   `boil:"started" json:"started,omitempty" toml:"started" yaml:"started,omitempty"`
	Changed    null.Time   `boil:"changed" json:"changed,omitempty" toml:"changed" yaml:"changed,omitempty"`
	ProtoData  null.JSON   `boil:"proto_data" json:"proto_data,omitempty" toml:"proto_data" yaml:"proto_data,omitempty"`
	Attributes null.JSON   `boil:"attributes" json:"attributes,omitempty" toml:"attributes" yaml:"attributes,omitempty"`
	Owner      string      `boil:"owner" json:"owner" toml:"owner" yaml:"owner"`
	Bulk       null.String `boil:"bulk" json:"bulk,omitempty" toml:"bulk" yaml:"bulk,omitempty"`

	R *tacacsSessionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tacacsSessionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TacacsSessionColumns = struct {
	ID         string
	Server     string
	User       string
	IPAddr     string
	Shared     string
	Started    string
	Changed    string
	ProtoData  string
	Attributes string
	Owner      string
	Bulk       string
}{
	ID:         "id",
	Server:     "server",
	User:       "user",
	IPAddr:     "ip_addr",
	Shared:     "shared",
	Started:    "started",
	Changed:    "changed",
	ProtoData:  "proto_data",
	Attributes: "attributes",
	Owner:      "owner",
	Bulk:       "bulk",
}

var TacacsSessionTableColumns = struct {
	ID         string
	Server     string
	User       string
	IPAddr     string
	Shared     string
	Started    string
	Changed    string
	ProtoData  string
	Attributes string
	Owner      string
	Bulk       string
}{
	ID:         "tacacs_sessions.id",
	Server:     "tacacs_sessions.server",
	User:       "tacacs_sessions.user",
	IPAddr:     "tacacs_sessions.ip_addr",
	Shared:     "tacacs_sessions.shared",
	Started:    "tacacs_sessions.started",
	Changed:    "tacacs_sessions.changed",
	ProtoData:  "tacacs_sessions.proto_data",
	Attributes: "tacacs_sessions.attributes",
	Owner:      "tacacs_sessions.owner",
	Bulk:       "tacacs_sessions.bulk",
}

// Generated where

var TacacsSessionWhere = struct {
	ID         whereHelperint64
	Server     whereHelpernull_String
	User       whereHelpernull_String
	IPAddr     whereHelpernull_String
	Shared     whereHelpernull_String
	Started    whereHelpernull_Time
	Changed    whereHelpernull_Time
	ProtoData  whereHelpernull_JSON
	Attributes whereHelpernull_JSON
	Owner      whereHelperstring
	Bulk       whereHelpernull_String
}{
	ID:         whereHelperint64{field: "\"tacacs_sessions\".\"id\""},
	Server:     whereHelpernull_String{field: "\"tacacs_sessions\".\"server\""},
	User:       whereHelpernull_String{field: "\"tacacs_sessions\".\"user\""},
	IPAddr:     whereHelpernull_String{field: "\"tacacs_sessions\".\"ip_addr\""},
	Shared:     whereHelpernull_String{field: "\"tacacs_sessions\".\"shared\""},
	Started:    whereHelpernull_Time{field: "\"tacacs_sessions\".\"started\""},
	Changed:    whereHelpernull_Time{field: "\"tacacs_sessions\".\"changed\""},
	ProtoData:  whereHelpernull_JSON{field: "\"tacacs_sessions\".\"proto_data\""},
	Attributes: whereHelpernull_JSON{field: "\"tacacs_sessions\".\"attributes\""},
	Owner:      whereHelperstring{field: "\"tacacs_sessions\".\"owner\""},
	Bulk:       whereHelpernull_String{field: "\"tacacs_sessions\".\"bulk\""},
}

// TacacsSessionRels is where relationship names are stored.
var TacacsSessionRels = struct {
}{}

// tacacsSessionR is where relationships are stored.
type tacacsSessionR struct {
}

// NewStruct creates a new relationship struct
func (*tacacsSessionR) NewStruct() *tacacsSessionR {
	return &tacacsSessionR{}
}

// tacacsSessionL is where Load methods for each relationship are stored.
type tacacsSessionL struct{}

var (
	tacacsSessionAllColumns            = []string{"id", "server", "user", "ip_addr", "shared", "started", "changed", "proto_data", "attributes", "owner", "bulk"}
	tacacsSessionColumnsWithoutDefault = []string{"owner"}
	tacacsSessionColumnsWithDefault    = []string{"id", "server", "user", "ip_addr", "shared", "started", "changed", "proto_data", "attributes", "bulk"}
	tacacsSessionPrimaryKeyColumns     = []string{"id"}
	tacacsSessionGeneratedColumns      = []string{}
)

type (
	// TacacsSessionSlice is an alias for a slice of pointers to TacacsSession.
	// This should almost always be used instead of []TacacsSession.
	TacacsSessionSlice []*TacacsSession
	// TacacsSessionHook is the signature for custom TacacsSession hook methods
	TacacsSessionHook func(context.Context, boil.ContextExecutor, *TacacsSession) error

	tacacsSessionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tacacsSessionType                 = reflect.TypeOf(&TacacsSession{})
	tacacsSessionMapping              = queries.MakeStructMapping(tacacsSessionType)
	tacacsSessionPrimaryKeyMapping, _ = queries.BindMapping(tacacsSessionType, tacacsSessionMapping, tacacsSessionPrimaryKeyColumns)
	tacacsSessionInsertCacheMut       sync.RWMutex
	tacacsSessionInsertCache          = make(map[string]insertCache)
	tacacsSessionUpdateCacheMut       sync.RWMutex
	tacacsSessionUpdateCache          = make(map[string]updateCache)
	tacacsSessionUpsertCacheMut       sync.RWMutex
	tacacsSessionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tacacsSessionAfterSelectMu sync.Mutex
var tacacsSessionAfterSelectHooks []TacacsSessionHook

var tacacsSessionBeforeInsertMu sync.Mutex
var tacacsSessionBeforeInsertHooks []TacacsSessionHook
var tacacsSessionAfterInsertMu sync.Mutex
var tacacsSessionAfterInsertHooks []TacacsSessionHook

var tacacsSessionBeforeUpdateMu sync.Mutex
var tacacsSessionBeforeUpdateHooks []TacacsSessionHook
var tacacsSessionAfterUpdateMu sync.Mutex
var tacacsSessionAfterUpdateHooks []TacacsSessionHook

var tacacsSessionBeforeDeleteMu sync.Mutex
var tacacsSessionBeforeDeleteHooks []TacacsSessionHook
var tacacsSessionAfterDeleteMu sync.Mutex
var tacacsSessionAfterDeleteHooks []TacacsSessionHook

var tacacsSessionBeforeUpsertMu sync.Mutex
var tacacsSessionBeforeUpsertHooks []TacacsSessionHook
var tacacsSessionAfterUpsertMu sync.Mutex
var tacacsSessionAfterUpsertHooks []TacacsSessionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TacacsSession) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TacacsSession) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TacacsSession) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TacacsSession) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TacacsSession) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TacacsSession) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TacacsSession) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TacacsSession) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TacacsSession) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tacacsSessionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTacacsSessionHook registers your hook function for all future operations.
func AddTacacsSessionHook(hookPoint boil.HookPoint, tacacsSessionHook TacacsSessionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tacacsSessionAfterSelectMu.Lock()
		tacacsSessionAfterSelectHooks = append(tacacsSessionAfterSelectHooks, tacacsSessionHook)
		tacacsSessionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tacacsSessionBeforeInsertMu.Lock()
		tacacsSessionBeforeInsertHooks = append(tacacsSessionBeforeInsertHooks, tacacsSessionHook)
		tacacsSessionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tacacsSessionAfterInsertMu.Lock()
		tacacsSessionAfterInsertHooks = append(tacacsSessionAfterInsertHooks, tacacsSessionHook)
		tacacsSessionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tacacsSessionBeforeUpdateMu.Lock()
		tacacsSessionBeforeUpdateHooks = append(tacacsSessionBeforeUpdateHooks, tacacsSessionHook)
		tacacsSessionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tacacsSessionAfterUpdateMu.Lock()
		tacacsSessionAfterUpdateHooks = append(tacacsSessionAfterUpdateHooks, tacacsSessionHook)
		tacacsSessionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tacacsSessionBeforeDeleteMu.Lock()
		tacacsSessionBeforeDeleteHooks = append(tacacsSessionBeforeDeleteHooks, tacacsSessionHook)
		tacacsSessionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tacacsSessionAfterDeleteMu.Lock()
		tacacsSessionAfterDeleteHooks = append(tacacsSessionAfterDeleteHooks, tacacsSessionHook)
		tacacsSessionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tacacsSessionBeforeUpsertMu.Lock()
		tacacsSessionBeforeUpsertHooks = append(tacacsSessionBeforeUpsertHooks, tacacsSessionHook)
		tacacsSessionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tacacsSessionAfterUpsertMu.Lock()
		tacacsSessionAfterUpsertHooks = append(tacacsSessionAfterUpsertHooks, tacacsSessionHook)
		tacacsSessionAfterUpsertMu.Unlock()
	}
}

// One returns a single tacacsSession record from the query.
func (q tacacsSessionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TacacsSession, error) {
	o := &TacacsSession{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for tacacs_sessions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TacacsSession records from the query.
func (q tacacsSessionQuery) All(ctx context.Context, exec boil.ContextExecutor) (TacacsSessionSlice, error) {
	var o []*TacacsSession

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TacacsSession slice")
	}

	if len(tacacsSessionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TacacsSession records in the query.
func (q tacacsSessionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count tacacs_sessions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q tacacsSessionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if tacacs_sessions exists")
	}

	return count > 0, nil
}

// TacacsSessions retrieves all the records using an executor.
func TacacsSessions(mods ...qm.QueryMod) tacacsSessionQuery {
	mods = append(mods, qm.From("\"tacacs_sessions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"tacacs_sessions\".*"})
	}

	return tacacsSessionQuery{q}
}

// FindTacacsSession retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTacacsSession(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*TacacsSession, error) {
	tacacsSessionObj := &TacacsSession{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"tacacs_sessions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, tacacsSessionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from tacacs_sessions")
	}

	if err = tacacsSessionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tacacsSessionObj, err
	}

	return tacacsSessionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TacacsSession) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tacacs_sessions provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tacacsSessionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tacacsSessionInsertCacheMut.RLock()
	cache, cached := tacacsSessionInsertCache[key]
	tacacsSessionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tacacsSessionAllColumns,
			tacacsSessionColumnsWithDefault,
			tacacsSessionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tacacsSessionType, tacacsSessionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tacacsSessionType, tacacsSessionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"tacacs_sessions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"tacacs_sessions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into tacacs_sessions")
	}

	if !cached {
		tacacsSessionInsertCacheMut.Lock()
		tacacsSessionInsertCache[key] = cache
		tacacsSessionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TacacsSession.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TacacsSession) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tacacsSessionUpdateCacheMut.RLock()
	cache, cached := tacacsSessionUpdateCache[key]
	tacacsSessionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tacacsSessionAllColumns,
			tacacsSessionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update tacacs_sessions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"tacacs_sessions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, tacacsSessionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tacacsSessionType, tacacsSessionMapping, append(wl, tacacsSessionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update tacacs_sessions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for tacacs_sessions")
	}

	if !cached {
		tacacsSessionUpdateCacheMut.Lock()
		tacacsSessionUpdateCache[key] = cache
		tacacsSessionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q tacacsSessionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for tacacs_sessions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for tacacs_sessions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TacacsSessionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tacacsSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"tacacs_sessions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, tacacsSessionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tacacsSession slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tacacsSession")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TacacsSession) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no tacacs_sessions provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tacacsSessionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tacacsSessionUpsertCacheMut.RLock()
	cache, cached := tacacsSessionUpsertCache[key]
	tacacsSessionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tacacsSessionAllColumns,
			tacacsSessionColumnsWithDefault,
			tacacsSessionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tacacsSessionAllColumns,
			tacacsSessionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert tacacs_sessions, could not build update column list")
		}

		ret := strmangle.SetComplement(tacacsSessionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(tacacsSessionPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert tacacs_sessions, could not build conflict column list")
			}

			conflict = make([]string, len(tacacsSessionPrimaryKeyColumns))
			copy(conflict, tacacsSessionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"tacacs_sessions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(tacacsSessionType, tacacsSessionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tacacsSessionType, tacacsSessionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert tacacs_sessions")
	}

	if !cached {
		tacacsSessionUpsertCacheMut.Lock()
		tacacsSessionUpsertCache[key] = cache
		tacacsSessionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TacacsSession record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TacacsSession) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TacacsSession provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tacacsSessionPrimaryKeyMapping)
	sql := "DELETE FROM \"tacacs_sessions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from tacacs_sessions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for tacacs_sessions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tacacsSessionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tacacsSessionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tacacs_sessions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tacacs_sessions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TacacsSessionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tacacsSessionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tacacsSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"tacacs_sessions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tacacsSessionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tacacsSession slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tacacs_sessions")
	}

	if len(tacacsSessionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TacacsSession) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTacacsSession(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TacacsSessionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TacacsSessionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tacacsSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"tacacs_sessions\".* FROM \"tacacs_sessions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tacacsSessionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TacacsSessionSlice")
	}

	*o = slice

	return nil
}

// TacacsSessionExists checks if the TacacsSession row exists.
func TacacsSessionExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"tacacs_sessions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if tacacs_sessions exists")
	}

	return exists, nil
}

// Exists checks if the TacacsSession row exists.
func (o *TacacsSession) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TacacsSessionExists(ctx, exec, o.ID)
}

// /////////////////////////////// BEGIN EXTENSIONS /////////////////////////////////
// Expose table columns
var (
	TacacsSessionAllColumns            = tacacsSessionAllColumns
	TacacsSessionColumnsWithoutDefault = tacacsSessionColumnsWithoutDefault
	TacacsSessionColumnsWithDefault    = tacacsSessionColumnsWithDefault
	TacacsSessionPrimaryKeyColumns     = tacacsSessionPrimaryKeyColumns
	TacacsSessionGeneratedColumns      = tacacsSessionGeneratedColumns
)

// GetID get ID from model object
func (o *TacacsSession) GetID() int64 {
	return o.ID
}

// GetIDs extract IDs from model objects
func (s TacacsSessionSlice) GetIDs() []int64 {
	result := make([]int64, len(s))
	for i := range s {
		result[i] = s[i].ID
	}
	return result
}

// GetIntfIDs extract IDs from model objects as interface slice
func (s TacacsSessionSlice) GetIntfIDs() []interface{} {
	result := make([]interface{}, len(s))
	for i := range s {
		result[i] = s[i].ID
	}
	return result
}

// ToIDMap convert a slice of model objects to a map with ID as key
func (s TacacsSessionSlice) ToIDMap() map[int64]*TacacsSession {
	result := make(map[int64]*TacacsSession, len(s))
	for _, o := range s {
		result[o.ID] = o
	}
	return result
}

// ToUniqueItems construct a slice of unique items from the given slice
func (s TacacsSessionSlice) ToUniqueItems() TacacsSessionSlice {
	result := make(TacacsSessionSlice, 0, len(s))
	mapChk := make(map[int64]struct{}, len(s))
	for i := len(s) - 1; i >= 0; i-- {
		o := s[i]
		if _, ok := mapChk[o.ID]; !ok {
			mapChk[o.ID] = struct{}{}
			result = append(result, o)
		}
	}
	return result
}

// FindItemByID find item by ID in the slice
func (s TacacsSessionSlice) FindItemByID(id int64) *TacacsSession {
	for _, o := range s {
		if o.ID == id {
			return o
		}
	}
	return nil
}

// FindMissingItemIDs find all item IDs that are not in the list
// NOTE: the input ID slice should contain unique values
func (s TacacsSessionSlice) FindMissingItemIDs(expectedIDs []int64) []int64 {
	if len(s) == 0 {
		return expectedIDs
	}
	result := []int64{}
	mapChk := s.ToIDMap()
	for _, id := range expectedIDs {
		if _, ok := mapChk[id]; !ok {
			result = append(result, id)
		}
	}
	return result
}

// InsertAll inserts all rows with the specified column values, using an executor.
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values
func (o TacacsSessionSlice) InsertAll(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	// Calculate the widest columns from all rows need to insert
	wlCols := make(map[string]struct{}, 10)
	for _, row := range o {
		wl, _ := columns.InsertColumnSet(
			tacacsSessionAllColumns,
			tacacsSessionColumnsWithDefault,
			tacacsSessionColumnsWithoutDefault,
			queries.NonZeroDefaultSet(tacacsSessionColumnsWithDefault, row),
		)
		for _, col := range wl {
			wlCols[col] = struct{}{}
		}
		if len(wlCols) == len(tacacsSessionAllColumns) {
			break
		}
	}
	wl := make([]string, 0, len(wlCols))
	for _, col := range tacacsSessionAllColumns {
		if _, ok := wlCols[col]; ok {
			wl = append(wl, col)
		}
	}

	var sql string
	vals := []interface{}{}
	for i, row := range o {

		if err := row.doBeforeInsertHooks(ctx, exec); err != nil {
			return 0, err
		}

		if i == 0 {
			sql = "INSERT INTO \"tacacs_sessions\" " + "(\"" + strings.Join(wl, "\",\"") + "\")" + " VALUES "
		}
		sql += strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), len(vals)+1, len(wl))
		if i != len(o)-1 {
			sql += ","
		}
		valMapping, err := queries.BindMapping(tacacsSessionType, tacacsSessionMapping, wl)
		if err != nil {
			return 0, err
		}

		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valMapping)...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, vals)
	}

	result, err := exec.ExecContext(ctx, sql, vals...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to insert all from tacacsSession slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by insertall for tacacs_sessions")
	}

	if len(tacacsSessionAfterInsertHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterInsertHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// InsertIgnoreAll inserts all rows with ignoring the existing ones having the same primary key values.
// NOTE: This function calls UpsertAll() with updateOnConflict=false and conflictColumns=<primary key columns>
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values
// IMPORTANT: if the table has `id` column of auto-increment type, this may not work as expected
func (o TacacsSessionSlice) InsertIgnoreAll(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	return o.UpsertAll(ctx, exec, false, tacacsSessionPrimaryKeyColumns, boil.None(), columns)
}

// UpsertAll inserts or updates all rows
// Currently it doesn't support "NoContext" and "NoRowsAffected"
// IMPORTANT: this will calculate the widest columns from all items in the slice, be careful if you want to use default column values
// IMPORTANT: if the table has `id` column of auto-increment type, this may not work as expected
func (o TacacsSessionSlice) UpsertAll(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	// Calculate the widest columns from all rows need to upsert
	insertCols := make(map[string]struct{}, 10)
	for _, row := range o {
		insert, _ := insertColumns.InsertColumnSet(
			tacacsSessionAllColumns,
			tacacsSessionColumnsWithDefault,
			tacacsSessionColumnsWithoutDefault,
			queries.NonZeroDefaultSet(tacacsSessionColumnsWithDefault, row),
		)
		for _, col := range insert {
			insertCols[col] = struct{}{}
		}
		if len(insertCols) == len(tacacsSessionAllColumns) {
			break
		}
	}
	insert := make([]string, 0, len(insertCols))
	for _, col := range tacacsSessionAllColumns {
		if _, ok := insertCols[col]; ok {
			insert = append(insert, col)
		}
	}

	update := updateColumns.UpdateColumnSet(
		tacacsSessionAllColumns,
		tacacsSessionPrimaryKeyColumns,
	)

	if updateOnConflict && len(update) == 0 {
		return 0, errors.New("models: unable to upsert tacacs_sessions, could not build update column list")
	}

	conflict := conflictColumns
	if len(conflict) == 0 {
		conflict = make([]string, len(tacacsSessionPrimaryKeyColumns))
		copy(conflict, tacacsSessionPrimaryKeyColumns)
	}

	buf := strmangle.GetBuffer()
	defer strmangle.PutBuffer(buf)

	columns := "DEFAULT VALUES"
	if len(insert) != 0 {
		columns = fmt.Sprintf("(%s) VALUES %s",
			strings.Join(insert, ", "),
			strmangle.Placeholders(dialect.UseIndexPlaceholders, len(insert)*len(o), 1, len(insert)),
		)
	}

	fmt.Fprintf(
		buf,
		"INSERT INTO %s %s ON CONFLICT ",
		"\"tacacs_sessions\"",
		columns,
	)

	if !updateOnConflict || len(update) == 0 {
		buf.WriteString("DO NOTHING")
	} else {
		buf.WriteByte('(')
		buf.WriteString(strings.Join(conflict, ", "))
		buf.WriteString(") DO UPDATE SET ")

		for i, v := range update {
			if i != 0 {
				buf.WriteByte(',')
			}
			quoted := strmangle.IdentQuote(dialect.LQ, dialect.RQ, v)
			buf.WriteString(quoted)
			buf.WriteString(" = EXCLUDED.")
			buf.WriteString(quoted)
		}
	}

	query := buf.String()
	valueMapping, err := queries.BindMapping(tacacsSessionType, tacacsSessionMapping, insert)
	if err != nil {
		return 0, err
	}

	var vals []interface{}
	for _, row := range o {

		if err := row.doBeforeUpsertHooks(ctx, exec); err != nil {
			return 0, err
		}

		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valueMapping)...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, vals)
	}

	result, err := exec.ExecContext(ctx, query, vals...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to upsert for tacacs_sessions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by upsert for tacacs_sessions")
	}

	if len(tacacsSessionAfterUpsertHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterUpsertHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// DeleteAllByPage delete all TacacsSession records from the slice.
// This function deletes data by pages to avoid exceeding Postgres limitation (max parameters: 65535)
func (s TacacsSessionSlice) DeleteAllByPage(ctx context.Context, exec boil.ContextExecutor, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// max number of parameters = 65535
	chunkSize := DefaultPageSize
	if len(limits) > 0 && limits[0] > 0 && limits[0] <= MaxPageSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.DeleteAll(ctx, exec)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].DeleteAll(ctx, exec)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

// UpdateAllByPage update all TacacsSession records from the slice.
// This function updates data by pages to avoid exceeding Postgres limitation (max parameters: 65535)
func (s TacacsSessionSlice) UpdateAllByPage(ctx context.Context, exec boil.ContextExecutor, cols M, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// max number of parameters = 65535
	// NOTE: len(cols) should not be too big
	chunkSize := DefaultPageSize
	if len(limits) > 0 && limits[0] > 0 && limits[0] <= MaxPageSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.UpdateAll(ctx, exec, cols)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].UpdateAll(ctx, exec, cols)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

// InsertAllByPage insert all TacacsSession records from the slice.
// This function inserts data by pages to avoid exceeding Postgres limitation (max parameters: 65535)
func (s TacacsSessionSlice) InsertAllByPage(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// max number of parameters = 65535
	chunkSize := MaxPageSize / reflect.ValueOf(&TacacsSessionColumns).Elem().NumField()
	if len(limits) > 0 && limits[0] > 0 && limits[0] < chunkSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.InsertAll(ctx, exec, columns)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].InsertAll(ctx, exec, columns)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

// InsertIgnoreAllByPage insert all TacacsSession records from the slice.
// This function inserts data by pages to avoid exceeding Postgres limitation (max parameters: 65535)
func (s TacacsSessionSlice) InsertIgnoreAllByPage(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// max number of parameters = 65535
	chunkSize := MaxPageSize / reflect.ValueOf(&TacacsSessionColumns).Elem().NumField()
	if len(limits) > 0 && limits[0] > 0 && limits[0] < chunkSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.InsertIgnoreAll(ctx, exec, columns)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].InsertIgnoreAll(ctx, exec, columns)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

// UpsertAllByPage upsert all TacacsSession records from the slice.
// This function upserts data by pages to avoid exceeding Postgres limitation (max parameters: 65535)
func (s TacacsSessionSlice) UpsertAllByPage(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, limits ...int) (int64, error) {
	length := len(s)
	if length == 0 {
		return 0, nil
	}

	// max number of parameters = 65535
	chunkSize := MaxPageSize / reflect.ValueOf(&TacacsSessionColumns).Elem().NumField()
	if len(limits) > 0 && limits[0] > 0 && limits[0] < chunkSize {
		chunkSize = limits[0]
	}
	if length <= chunkSize {
		return s.UpsertAll(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns)
	}

	rowsAffected := int64(0)
	start := 0
	for {
		end := start + chunkSize
		if end > length {
			end = length
		}
		rows, err := s[start:end].UpsertAll(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns)
		if err != nil {
			return rowsAffected, err
		}

		rowsAffected += rows
		start = end
		if start >= length {
			break
		}
	}
	return rowsAffected, nil
}

///////////////////////////////// END EXTENSIONS /////////////////////////////////
