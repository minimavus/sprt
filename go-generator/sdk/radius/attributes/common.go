package attributes

var (
	CommonRequest = []RadiusAttribute{
		Build(ID("Acct-Session-Id"),
			Value("$SESSION_ID$"),
			RFC2866,
			NonRemovable()),
		Build(ID("Calling-Station-Id"),
			Value("$MAC$"),
			RFC2865,
			Overridable(),
			CustomValues([]string{"$MAC$"})),
		Build(ID("Called-Station-Id"),
			Value("00-00-00-FF-FF-FF"),
			RFC2865,
			Overridable()),
		Build(ID("NAS-IP-Address"),
			Value("$NAS_IP$"),
			RFC2865,
			Overridable(),
			CustomValues([]string{"$NAS_IP$"}),
			V4Only),
		Build(ID("NAS-IPv6-Address"),
			Value("$NAS_IP$"),
			RFC3162,
			Overridable(),
			CustomValues([]string{"$NAS_IP$"}),
			V6Only),
		Build(ID("NAS-Port-Type"),
			Value("$PORT_TYPE$"),
			RFC2865,
			Overridable(),
			CustomValues([]string{"$PORT_TYPE$"})),
		Build(ID("Message-Authenticator"),
			Calculate,
			RFC2869,
			Overridable(),
			CustomValues([]string{"Calculate"})),
		Build(ID("Framed-MTU"),
			Value("$MTU$"),
			RFC2865,
			Overridable(),
			CustomValues([]string{"$MTU$"})),
	}

	CommonAccounting = []RadiusAttribute{
		Build(ID("Acct-Authentic"),
			Value("RADIUS"),
			RFC2866,
			Overridable()),
		Build(ID("Acct-Session-Id"),
			CopyLatestValue,
			RFC2866,
			Overridable(),
			CopyValues),
		Build(ID("Acct-Status-Type"),
			Value("Start"),
			RFC2866,
			Overridable()),
		Build(ID("Called-Station-Id"),
			CopyLatestValue,
			RFC2865,
			Overridable(),
			CopyValues),
		Build(ID("Calling-Station-Id"),
			CopyLatestValue,
			RFC2865,
			Overridable(),
			CopyValues),
		Build(ID("Class"),
			CopyFromResponse,
			RFC2865,
			Overridable(),
			CopyValues),
		Build(ID("Framed-IP-Address"),
			Value("$IP$"),
			RFC2865,
			Overridable(),
			CustomValues([]string{"$IP$"})),
		Build(ID("NAS-IP-Address"),
			CopyLatestValue,
			RFC2865,
			Overridable(),
			CopyValues,
			V4Only),
		Build(ID("NAS-IPv6-Address"),
			CopyLatestValue,
			RFC3162,
			Overridable(),
			CopyValues,
			V6Only),
		Build(ID("NAS-Port-Type"),
			CopyLatestValue,
			RFC2865,
			Overridable(),
			CopyValues),
		Build(ID("Service-Type"),
			CopyLatestValue,
			RFC2865,
			Overridable(),
			CopyValues),
		Build(ID("User-Name"),
			CopyFromResponse,
			RFC2865,
			Overridable(),
			CopyValues),
	}
)
